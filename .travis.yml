language: java
jdk:
  - openjdk8

env:
  - AWS_REGION=us-east-1
cache:
  directories:
  - "$HOME/.cache"

deploy:
  provider: script
  script: "cp .travis.settings.xml $HOME/.m2/settings.xml && mvn deploy"
  on:
    tags: true
